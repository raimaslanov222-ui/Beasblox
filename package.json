{
  "name": "beasblox-app",
  "version": "1.0.0",
  "description": "Mobile Game Platform",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p www && cp index.html www/",
    "add-android": "npx cap add android",
    "sync-android": "npx cap sync android",
    "full-build": "npm run build && npx cap copy && npx cap sync"
  },
  "keywords": [],
  "author": "raimaslanov222@gmail.com",
  "license": "ISC",
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/android": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
